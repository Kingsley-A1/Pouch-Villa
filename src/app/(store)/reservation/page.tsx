import Image from "next/image";
import { notFound } from "next/navigation";
import { Info } from "@phosphor-icons/react/dist/ssr";
import { ReservationForm } from "@/components/reservation-form";
import { getProductBySlug } from "@/lib/db";
import { parseVariants, toSingle } from "@/lib/utils";
export const dynamic = "force-dynamic";
export default async function ReservationPage({ searchParams }: { searchParams: Promise<Record<string,string|string[]|undefined>> }) { const params = await searchParams; const slug = toSingle(params.product); const product = getProductBySlug(slug); if (!product) notFound(); const initialModel = toSingle(params.model); const initialVariant = toSingle(params.variant); return <section className="section-space"><div className="container-shell grid gap-10 lg:grid-cols-[.65fr_1.35fr]"><aside><div className="relative aspect-square overflow-hidden rounded-3xl bg-[#f6f3f1]"><Image src={product.image} alt={`${product.name} demonstration case`} fill sizes="(max-width:1024px) 100vw, 35vw" className="object-cover" /></div><h1 className="mt-5 text-2xl font-bold">Reserve {product.name}</h1><p className="mt-3 flex gap-2 text-sm leading-6 text-zinc-500"><Info size={19} className="mt-0.5 shrink-0" /> A reservation is not a purchase or live-stock guarantee. Staff verifies details next.</p></aside><div className="rounded-3xl border border-[#e8e3df] p-6 sm:p-9"><p className="eyebrow">Pickup reservation</p><h2 className="section-title mt-3 mb-7">Confirm the fit, then prepare pickup.</h2><ReservationForm productSlug={product.slug} devices={product.devices || []} initialModel={initialModel} variants={parseVariants(product.variants_json)} initialVariant={initialVariant} /></div></div></section>; }
