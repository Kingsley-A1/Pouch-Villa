import Link from "next/link";
import { CheckCircle } from "@phosphor-icons/react/dist/ssr";
import { toSingle } from "@/lib/utils";
export default async function ReservationSuccessPage({ searchParams }: { searchParams: Promise<Record<string,string|string[]|undefined>> }) { const reference = toSingle((await searchParams).reference); return <section className="section-space"><div className="container-shell"><div className="mx-auto max-w-2xl rounded-[2rem] border border-[#e8e3df] p-8 text-center sm:p-12"><CheckCircle size={58} weight="fill" className="mx-auto text-emerald-600" /><p className="eyebrow mt-6">Reservation prepared</p><h1 className="section-title mt-3">Your confirmation reference is ready.</h1><div className="my-7 rounded-2xl bg-[#f6f3f1] p-5 font-mono text-xl font-bold">{reference || "Reference unavailable"}</div><p className="leading-7 text-zinc-600">In the prototype, staff can now see and manage this request from the protected dashboard. No real message or payment has been sent.</p><div className="mt-8 flex flex-wrap justify-center gap-3"><Link className="button-primary" href="/shop">Continue shopping</Link><Link className="button-ghost" href="/">Back home</Link></div></div></div></section>; }
