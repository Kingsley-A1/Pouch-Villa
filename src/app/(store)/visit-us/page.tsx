import Image from "next/image";
import { Clock, MapPin, WarningCircle } from "@phosphor-icons/react/dist/ssr";
import { Breadcrumbs } from "@/components/breadcrumbs";
import { WhatsAppEnquiry } from "@/components/whatsapp-enquiry";
import { getSetting } from "@/lib/db";
export const dynamic = "force-dynamic";
export default function VisitUsPage() { const address = process.env.NEXT_PUBLIC_STORE_ADDRESS || getSetting("store_address"); const hours = process.env.NEXT_PUBLIC_STORE_HOURS || getSetting("opening_hours"); const whatsapp = process.env.NEXT_PUBLIC_WHATSAPP_NUMBER || getSetting("whatsapp_number"); return <><Breadcrumbs trail={[{ label: "Visit us" }]} /><section className="section-space"><div className="container-shell"><div className="grid overflow-hidden rounded-[2rem] border border-[#e8e3df] lg:grid-cols-2"><div className="relative min-h-[520px]"><Image src="/images/phone-cases-display.jpg" alt="A spread of phone cases in assorted colours and patterns" fill priority sizes="(max-width:1024px) 100vw,50vw" className="object-cover" /></div><div className="p-7 sm:p-10 lg:p-14"><p className="eyebrow">Pouch Villa, Calabar</p><h1 className="section-title mt-3">Visit the physical store.</h1><p className="mt-5 leading-7 text-zinc-600">Photography is illustrative. The exact location and hours are intentionally not guessed.</p><div className="mt-8 grid gap-6"><div className="flex gap-3"><MapPin size={24} className="text-[#e30613]" /><div><p className="font-bold">Address</p><p className="mt-1 text-zinc-600">{address || "Awaiting Pouch Villa confirmation"}</p></div></div><div className="flex gap-3"><Clock size={24} className="text-[#e30613]" /><div><p className="font-bold">Opening hours</p><p className="mt-1 text-zinc-600">{hours || "Awaiting Pouch Villa confirmation"}</p></div></div>{!address ? <div className="flex gap-3 rounded-2xl bg-amber-50 p-4 text-sm leading-6 text-amber-950"><WarningCircle size={21} className="shrink-0" /> Map directions will be enabled only after an exact approved address is configured.</div> : null}</div><div className="mt-8"><WhatsAppEnquiry number={whatsapp} message="Hello Pouch Villa, please confirm your official store address and opening hours before I visit." label="Prepare visit enquiry" /></div></div></div></div></section></>; }
